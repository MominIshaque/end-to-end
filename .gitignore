# Local .terraform directories
**/.terraform/*
**/.terraform.tfstate*

# Terraform state files
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Terraform plan files
*.tfplan
*.tfplan.*

# Override files (useful locally but not for sharing)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Variable files containing secrets (keep safe)
*.tfvars
*.tfvars.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Lock file (can be committed if team wants dependency locking)
# Uncomment the next line if you don’t want to commit the lock file
#.terraform.lock.hcl

